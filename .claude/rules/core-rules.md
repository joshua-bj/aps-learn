# Core Rules
## python
Have a vertual environment for this project, at folder `./venv`. You can use `source ./venv/bin/activate` to activate the environment, and `deactivate` to exit the environment. After active the vertual environment, you can use `python xxxx.py` to run the python code. 